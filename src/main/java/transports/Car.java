package transports;

import vehicles.Vehicle;


public class Car extends Vehicle {
        public Car (String brand, String model, String regPlate, int topSpeed, int price){
                    super(brand, model, regPlate, topSpeed, price);
        }
}
